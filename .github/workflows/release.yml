name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Create Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        body: |
          ## 🚀 KF13 Open Datasets Release
          
          ### 📊 What's Included
          - 14 comprehensive JSON datasets
          - 3,000+ verified data entries
          - Complete API documentation
          - Community contribution guidelines
          
          ### 🌟 Key Features
          - Educational institutions database
          - Physics competitions and events
          - Research labs and facilities
          - Career pathways and opportunities
          - Innovation ecosystem mapping
          
          ### 🔗 Quick Access
          - **API Base**: https://klubfisika.github.io/datasets/
          - **Documentation**: https://github.com/klubfisika/datasets#readme
          - **Contributing**: https://github.com/klubfisika/datasets/blob/main/CONTRIBUTING.md
          
          ### 📈 Usage Examples
          ```bash
          # Test API endpoint
          curl https://klubfisika.github.io/datasets/v1/institutions-diy.json
          
          # Python usage
          import requests
          data = requests.get('https://klubfisika.github.io/datasets/v1/competitions-indonesia.json').json()
          ```
          
          **🇮🇩 Made with ❤️ by the Indonesian Physics Community**
        draft: false
        prerelease: false
